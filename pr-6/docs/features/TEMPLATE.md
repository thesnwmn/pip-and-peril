# NNN · Feature Title

> Copy this file to `docs/features/NNN-short-title.md` and fill it in. Written by the **Designer**.
> When the feature ships, the Engineer moves this file to `docs/features/history/`.

**Status:** NEEDS SPEC | READY
**Source idea:** Idea NNN (`IDEAS.md`), or "manager request"
**Depends on:** feature NNN, decision Dn (or "none")

## Summary

One paragraph: what the feature is and the value it delivers to the player.

## Acceptance criteria

The contract. Each criterion is concrete and testable — the Engineer builds to these and the
Reviewer checks against them.

1. …
2. …
3. …

## Scope / non-goals

What this item explicitly does **not** include (deferred to later features or out of scope).

- …

## Design detail

How it should work: data shapes, interfaces, states, screens, and edge cases. Interfaces and shapes
are welcome; full implementation code is not — that's the Engineer's call.

- **Data / state:** …
- **Behaviour:** …
- **Edge cases:** …

## Visual design

*Include this section for any feature with a visible screen or UI component. Remove it for purely
logic/data features.*

**Layout wireframe**

```
┌──────────────────────────────┐
│  (ASCII or describe the       │
│   layout of key elements)     │
└──────────────────────────────┘
```

**Color tokens**

New named tokens introduced by this feature. Extend the palette established in `docs/concept.md`;
don't invent new values without a rationale.

| Token | Value | Used for |
|---|---|---|
| … | … | … |

**Typography / sizing**

Note any new font sizes, weights, or spacing that differ from what already exists on screen.

## Open questions

Anything the manager still needs to decide. **An item with blocking open questions stays
`NEEDS SPEC`, not `READY`.**

- …
